<%= form_with(model: user_project) do |form| %>
  <% if user_project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_project.errors.count, "error") %> prohibited this user_project from being saved:</h2>

      <ul>
        <% user_project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :is_leader,"Leader", style: "display: block" %>
    <%= form.check_box :is_leader, checked: true %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.collection_select :user_id, User.all, :id, :username %>
  </div>

  <div>
    <%= form.label :project_id, style: "display: block" %>
    <%= form.collection_select :project_id, Project.all, :id, :name %>
  </div>

  <div>
    <%= form.label :time_assigned, style: "display: block" %>
    <%= form.date_field :time_assigned %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
